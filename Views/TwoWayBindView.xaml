<UserControl x:Class="BindingsAndTriggers.Views.TwoWayBindView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lib="clr-namespace:LocalizationLib;assembly=LocalizationLib">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
        <StackPanel MaxWidth="700" HorizontalAlignment="Left">

            <TextBlock Style="{StaticResource PageTitle}"
                       Text="{Binding [TwoWay_Title], Source={x:Static lib:Translator.Instance}}"/>

            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}"
                               Text="{Binding [TwoWay_HowTitle], Source={x:Static lib:Translator.Instance}}"/>
                    <TextBlock TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}"
                               Text="{Binding [TwoWay_HowDesc], Source={x:Static lib:Translator.Instance}}"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}"
                               Text="{Binding [TwoWay_MultiTitle], Source={x:Static lib:Translator.Instance}}"/>
                    <TextBlock Style="{StaticResource Label}"
                               Text="{Binding [TwoWay_MultiHint], Source={x:Static lib:Translator.Instance}}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding SharedText, Mode=TwoWay}"
                                 ToolTip="{Binding [TwoWay_Field1], Source={x:Static lib:Translator.Instance}}"/>
                        <TextBox Grid.Column="2" Text="{Binding SharedText, Mode=TwoWay}"
                                 ToolTip="{Binding [TwoWay_Field2], Source={x:Static lib:Translator.Instance}}"/>
                    </Grid>
                    <TextBlock Style="{StaticResource Label}" Margin="0,10,0,4"
                               Text="{Binding [TwoWay_VMValue], Source={x:Static lib:Translator.Instance}}"/>
                    <TextBlock Style="{StaticResource ResultText}"
                               Text="{Binding SharedText, Mode=OneWay}" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}"
                               Text="{Binding [TwoWay_SliderTitle], Source={x:Static lib:Translator.Instance}}"/>
                    <TextBlock Style="{StaticResource Label}"
                               Text="{Binding [TwoWay_SliderHint], Source={x:Static lib:Translator.Instance}}"/>
                    <Grid Margin="0,4,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="80"/>
                        </Grid.ColumnDefinitions>
                        <Slider Grid.Column="0" Minimum="-30" Maximum="50"
                                Value="{Binding Temperature, Mode=TwoWay}"
                                VerticalAlignment="Center"/>
                        <TextBox Grid.Column="2"
                                 Text="{Binding Temperature, Mode=TwoWay, StringFormat=F1}"
                                 TextAlignment="Center"/>
                    </Grid>
                    <TextBlock Margin="0,10,0,0" FontSize="16" FontWeight="Bold"
                               Foreground="{StaticResource PrimaryBrush}"
                               Text="{Binding Temperature, StringFormat={}{0:F1}, Mode=OneWay}"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}"
                               Text="{Binding [TwoWay_ComboTitle], Source={x:Static lib:Translator.Instance}}"/>
                    <TextBlock Style="{StaticResource Label}"
                               Text="{Binding [TwoWay_ComboHint], Source={x:Static lib:Translator.Instance}}"/>
                    <ComboBox ItemsSource="{Binding Colors}"
                              SelectedItem="{Binding SelectedColor, Mode=TwoWay}"
                              Width="200" HorizontalAlignment="Left"/>
                    <Border Margin="0,12,0,0" Width="200" Height="50"
                            CornerRadius="6" HorizontalAlignment="Left">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="#E53935"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedColor}" Value="Красный">
                                        <Setter Property="Background" Value="#E53935"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SelectedColor}" Value="Зелёный">
                                        <Setter Property="Background" Value="#43A047"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SelectedColor}" Value="Синий">
                                        <Setter Property="Background" Value="#1E88E5"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SelectedColor}" Value="Жёлтый">
                                        <Setter Property="Background" Value="#FDD835"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SelectedColor}" Value="Фиолетовый">
                                        <Setter Property="Background" Value="#8E24AA"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center"
                                   FontWeight="Bold" FontSize="14">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="White"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedColor}" Value="Жёлтый">
                                            <Setter Property="Foreground" Value="#333333"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                            <Run Text="{Binding SelectedColor, Mode=OneWay}"/>
                        </TextBlock>
                    </Border>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
